name: fun_ds
channels:
  - conda-forge
  - nodefaults
dependencies:
  # defaults
  - python>=3.8
  - make
  - numpydoc
  - pip
  - pre-commit
  - pytest>=6  # Adds --import-mode option
  - pytest-cov
  - setuptools-scm
  - sphinx
  - sphinxcontrib-apidoc
  - sphinx_rtd_theme
  # data
  - pandas
  - sqlalchemy
  - sqlite
  - kaggle
  # interactive
  - ipykernel
